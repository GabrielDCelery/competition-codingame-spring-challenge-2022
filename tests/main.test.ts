import { expect } from 'chai';
import { describe, it } from 'mocha';

describe('main', () => {
    it('does something', () => {
        // Given

        // When

        // Then
        expect(true).to.deep.equal(true);
    });
});
